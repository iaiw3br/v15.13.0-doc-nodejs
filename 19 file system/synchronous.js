/*
* Синхронная операция блокирует цикл событии до тех пор, пока операция не завершится или не завершится с ошибкой.
* */

/*
* fs.accessSync(path[, mode])
* test permissions for the file or directory.
* */


/*
* fs.appendFileSync(path, data[, options])
* append data to file.
* */


/*
* fs.chmodSync(path, mode)
* TODO: Не нашёл примеров, вернуться.
* */


/*
* fs.chownSync(path, uid, gid)
* change owner of a file.
* */


/*
* fs.closeSync(fd)
* close file.
* */


/*
* fs.copyFileSync(src, dest[, mode])
* copies src to dest.
*
*
* fs.existsSync(path)
* returns true if path exists, false otherwise.
*
*
* fs.fchmodSync(fd, mode)
* sets the permissions on the file.
*
*
* fs.fchownSync(fd, uid, gid)
* sets the owner of the file.
*
*
* fs.fdatasyncSync(fd)
* TODO: Не нашёл примеров, вернуться.
*
*
* fs.fstatSync(fd[, options])
* show stats for the file.
*
*
* fs.fsyncSync(fd)
* request that all data for the open file.
*
*
* fs.ftruncateSync(fd[, len])
* truncate file.
*
*
* fs.futimesSync(fd, atime, mtime)
* change atime, mtime
*
*
* fs.lchownSync(path, uid, gid)
* set owner for the path.
*
*
* fs.lutimesSync(path, atime, mtime)
* change the file timestamps
*
*
* fs.linkSync(existingPath, newPath)
* create a new link from the existingPath to newPath
*
*
* fs.lstatSync(path[, options])
* show stats
*
*
* fs.mkdirSync(path[, options])
* create directory
*
*
* fs.mkdtempSync(prefix[, options])
* returns the created directory path.
*
*
* fs.opendirSync(path[, options])
* open directory.
*
*
* fs.openSync(path[, flags, mode])
* TODO: Не нашёл примеров, вернуться.
*
*
* fs.readdirSync(path[, options])
* reads content of the directory
*
*
* fs.readFileSync(path[, options])
* returns the contents of the file.
*
*
* fs.readlinkSync(path[, options])
* returns the symbolic link.
*
*
* fs.readSync(fd, buffer, offset, length, position)
* return the number of bytesRead.
*
*
* fs.realpathSync(path[, options])
* returns the resolved pathname
*
*
* fs.realpathSync.native(path[, options])
* returns path that can be converted to utf8
*
*
* fs.renameSync(oldPath, newPath)
* renames the file from oldPath to newPath.
*
*
* fs.rmdirSync(path[, options])
* remove directory.
*
*
* fs.rmSync(path[, options])
* removes files and directories.
*
*
* fs.statSync(path[, options])
* shows stat of the file.
*
*
* fs.truncateSync(path[, len])
* truncates the file.
*
*
* fs.writeFileSync(file, data[, options])
* fs.writeSync(fd, buffer[, offset[, length[, position]]])
* fs.writeSync(fd, string[, position[, encoding]])
* write file.
* */
